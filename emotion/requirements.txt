# Pinned to versions that match Colab's default stack (May 2026) where possible.
# Colab already ships torch/torchvision; we still pin so local installs match.
torch>=2.2
torchvision>=0.17
numpy>=1.26
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.4
Pillow>=10.0
tqdm>=4.66
kaggle>=1.6
